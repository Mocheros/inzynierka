<%= form_with(model: tournament) do |form| %>
  <% if tournament.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tournament.errors.count, "error") %> prohibited this tournament from being saved:</h2>

      <ul>
        <% tournament.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :format, style: "display: block" %>
    <%= form.select :format, options_for_select(["Liga", "Puchar"]) %>
  </div>

  <div>
    <%= form.label :number_of_teams, style: "display: block" %>
    <%= form.select :number_of_teams, options_for_select(1..64) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
